program: train.sweep
method: bayes
metric:
  name: mean_dir_acc
  goal: maximize
parameters:
  lr:
    min: 0.0001
    max: 0.1
  mixtures:
    min: 50
    max: 1000
  train_size:
    min: 1
    max: 8

  optimizer:
    values: ["adam", "sgd"]

early_terminate:
  type: hyperband
  min_iter: 2